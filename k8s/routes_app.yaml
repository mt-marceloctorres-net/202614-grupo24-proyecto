apiVersion: v1
kind: Secret
metadata:
  name: routes-db-secret
  namespace: default
type: Opaque
stringData:
  POSTGRES_PASSWORD: postgres

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: routes-db
  namespace: default
  labels:
    app: routes-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: routes-db
  template:
    metadata:
      labels:
        app: routes-db
    spec:
      containers:
        - name: routes-db
          image: postgres:16
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: routes_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: routes-db-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: routes-db-storage
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: routes-db-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: routes-db-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: routes-db
  ports:
    - port: 5432
      targetPort: 5432

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: routes-app
  namespace: default
  labels:
    app: routes-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: routes-app
  template:
    metadata:
      labels:
        app: routes-app
    spec:
      initContainers:
        - name: wait-for-routes-db
          image: busybox:1.36
          command:
            - sh
            - -c
            - until nc -z routes-db-service 5432; do echo waiting for routes-db; sleep 2; done
      containers:
        - name: routes-app
          image: routes_app:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9000
          env:
            - name: DB_HOST
              value: routes-db-service
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: routes_db
            - name: DB_USER
              value: postgres
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: routes-db-secret
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /routes/ping
              port: 9000
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /routes/ping
              port: 9000
            initialDelaySeconds: 10
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: routes-app-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: routes-app
  ports:
    - port: 80
      targetPort: 9000
      nodePort: 30002
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: routes-network
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: routes-db
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: routes-app
      ports:
        - protocol: TCP
          port: 5432